<% @card_title = 'Consulta via Elastick' %>

<%= form_with url: search_path, data: { turbo_frame: :results } do |f| %>
  <%= f.label :Buscar %>
  <%= f.text_field :search, 
                                            oninput: 'this.form.requestSubmit()',
                                            autofocus: true,
                                            autocomplete: 'off' %>
<% end %>
<%= turbo_frame_tag :results do %>
  <div class="table-responsive">
    <table class="table table-striped table-bordered table-sm">
      <thead>
        <tr>
          <th>Nome</th>
          <th>CPF</th>
          <th>email</th>
          <th>Endereço</th>
          <th>Cidade</th>
        </tr>
      </thead>
      <tbody id="citizen_table">
        <%= render partial: 'citizen', collection: @citizens %>
      </tbody>
    </table>
  </div>
<% end %>
